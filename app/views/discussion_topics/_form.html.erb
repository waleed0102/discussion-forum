<%= form_for(@discussion_topic, url: campaign_discussion_topics_path(@campaign)) do |f| %>
  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: 'form-control' %>
    <br>
    <%= f.submit 'Create', class: 'btn btn-success' %>
  </div>
<% end %>
